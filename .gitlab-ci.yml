image: docker:latest

services:
  - docker:dind

before_script:
  - echo "Setting up Docker..."
  - docker login -u gitlab-ci-token -p $CI_JOB_TOKEN $DOCKER_REGISTRY

stages:
  - build
  - deploy

variables:
  DOCKER_REGISTRY: registry.gitlab.com
  FRONTEND_IMAGE: $DOCKER_REGISTRY/rezav/rezaverse/frontend

build_frontend:
  stage: build
  script:
    - echo "Building Docker image..."
    - >
      docker build \
        --build-arg REACT_APP_API_URL="https://rezav.gitlab.io/rezaverse/api" \
        --build-arg REACT_APP_DOMAIN_NAME="https://rezav.gitlab.io/rezaverse" \
        -t $FRONTEND_IMAGE ./frontend
    - echo "Pushing Docker image to GitLab Container Registry..."
    - docker push $FRONTEND_IMAGE
  only:
    - main

pages:
  stage: deploy
  script:
    - echo "Deploying frontend to GitLab Pages..."
    - mkdir -p public
    - docker run --rm $FRONTEND_IMAGE tar -cf - /usr/share/nginx/html | tar -xf - -C public
  artifacts:
    paths:
      - public
  only:
    - main